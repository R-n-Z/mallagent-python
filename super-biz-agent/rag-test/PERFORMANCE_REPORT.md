# RAG 性能报告

**最后更新**: 2026-05-31（三层漏斗重构完成）  
**架构**: ReAct（单 Agent + 判定框架 prompt）  
**嵌入模型**: DashScope text-embedding-v4 (1024维)  
**向量库**: Milvus 2.5.10  
**索引**: 全部 HNSW

---

## 一、检索性能总览

### 1.1 当前指标 (2026-05-31)

| 指标 | FAQ 向量 | 精确匹配 | 混合检索(文本匹配) |
|------|---------|---------|------------------|
| 用例数 | 60 | 112 | 103 |
| Recall@1 | 88.3% | **59.5%** | **58.0%** |
| Recall@3 | **100.0%** | **65.3%** | **79.0%** |
| Recall@5 | 100.0% | 65.3% | **81.5%** |
| Hit@3 | 86.7% | **59.8%** | **84.5%** |
| MRR@3 | 0.81 | 0.58 | **0.75** |
| Failed | 0 | 36 | 16 |

> 混合检索使用文本匹配评估（同 FAQ 模式，子串包含即命中）。`evaluate.py` 分数阈值模式在权重更新后失效（需重新标定 `expectedMinScore`）。

### 1.2 精确路三层漏斗 (112 用例)

| 层级 | 精度 | 覆盖 | 说明 |
|------|------|------|------|
| L1 exact(contains) | 76% | 33 用例 | 确定性命中 |
| L2 synonym | **91%** | 30 用例 | 最大增量贡献 |
| L3 fuzzy(≥4字拼写) | 40% | 4 用例 | 拼写纠错安全网 |
| **总计** | | **67/112 (60%)** | |

### 1.3 Agent 层指标

| 指标 | 值 | 说明 |
|------|-----|------|
| 稳定性 | **100%** | 5 用例 × 3 轮，分数范围 0.000 |
| 延迟 P50 | 195ms | 单次检索 |
| 延迟 P95 | 856ms | 含 embedding API 波动 |
| LRU 缓存 | **184x** | 562ms → 3ms |
| 输出精简 | **~70%** | 600 → 180 chars |
| 冷启动退化 | **0** | HNSW 热/冷完全一致 |

### 1.3 FAQ 按类别

| 类别 | R@1 | R@3 | Hit@3 |
|------|-----|-----|-------|
| 精确匹配 | 100% | 100% | 100% |
| 语义相似-支付 | 86% | 100% | 100% |
| 语义相似-退换货 | 100% | 100% | 100% |
| 语义相似-物流 | 100% | 100% | 100% |
| 语义相似-售后 | 100% | 100% | 100% |
| 商品查询 | 33-100% | 100% | 100% |
| 长查询/短查询/模糊 | 33-100% | 100% | 100% |

---

## 二、混合检索权重演化

| 版本 | vw | kw | vt | vtk | 数据量 | 状态 |
|------|-----|-----|-----|-----|-----|------|
| v1 初始 | 0.80 | 1.00 | 0.40 | 5 | — | 已废弃 |
| **v2 已验证** | **1.31** | **0.65** | **0.26** | **6** | 25 条 | **生产中** |
| v3 训练（废弃） | 0.61 | 0.52 | 0.26 | 6 | 96 条 | 实测退化，已回滚 |

**v3 废弃原因**：差分进化在模拟器中表现好（R@3=116.5%），但训练数据来自旧权重检索结果，模拟器的 merge 仿真与实际 Java 逻辑存在偏差。实际部署后分数阈值评估 Correctness 显著下降。当前保持 v2 经验证权重。

训练方法: 差分进化 + 5折交叉验证, 目标函数 Recall@3

---

## 三、精确路失效根因与修复

### 3.1 优化前（96 条用例）

| 类别 | 数量 | 占比 | 可修复 |
|------|------|------|--------|
| 精确子串命中 | 25 | 26% | — |
| 同义词命中 | 16 | 17% | — |
| 编辑距离命中 | 0 | 0% | 结构性问题 |
| 正确0命中（无关/否定） | 9 | 9% | — |
| **同义词词典缺失** | **18** | **19%** | ✓ 已修复 |
| **语义鸿沟（无共同词汇）** | **28** | **29%** | few-shot 兜底 |

### 3.2 同义词补充修复明细

| 规则 | 新增同义词 | 修复用例 |
|------|----------|---------|
| 起诉 | "收传票" "等着被告" | R082 ✓ |
| 过敏 | "起红点" "发红" "发烫" "又痒又痛" "起疹子" | R090 ✓, R091 ✓ |
| 曝光 | "让全网都知道" "发到微博上" | R100 ✓, R102 ✓ |
| 受伤 | "被划了一下" "差点烫到" "冒烟" | R130 ✓, R132 ✓ |
| 欺诈 | "期诈" "欺骗" | R181 ✓ |
| AA-001 | "冲动消费" "不太合适" "不太适合" | 待重启验证 |
| AA-002 | "脱线" "不亮" "拉链坏了" "洗了就掉色" | 待重启验证 |
| AA-003 | "功能不符" "写的蓝牙没有" | 待重启验证 |
| AA-004 | "屏幕被快递摔裂了" "摔变形了" | 待重启验证 |
| SR-001 | "变质" "臭了" "发霉" | 待重启验证 |

### 3.3 剩余语义盲区（混合检索 + 同义词仍未覆盖）

以下场景需推理层 few-shot 兜底：

| 盲区 | 涉及用例 | 修复方式 |
|------|---------|---------|
| 掉色/线头/做工差 → 质量问题 | R152, R153, R261 | few-shot 语义映射 |
| 色差/颜色不对 → 与描述不符 | R160, R164, R263 | few-shot 语义映射 |
| 下错单/多拍 → 不想要了 | R141, R142 | rules.json 同义词 + few-shot |
| 功能异常描述 → 质量问题 | R151 | rules.json 同义词 + few-shot |
| 物流损坏描述 → 物流损坏 | R170-R172 | rules.json 同义词 + few-shot |

---

## 四、权重训练经验教训

1. **训练数据必须从实际权重配置的检索结果中采集**，不能跨权重版本复用
2. **差分进化模拟器**的 merge 逻辑与 Java `HybridSearchTools` 存在偏差（主要在于去重排序的边界条件）
3. **评估器的分数阈值**（`expectedMinScore`）与权重强耦合——改变权重后阈值需重新标定
4. **推荐流程**：修改权重 → 跑全量检索 → 重新标定每条用例的 `expectedMinScore` → 重新训练

---

## 五、Agent 层优化

### 5.1 LRU 缓存

`ReturnRuleAgentService.analyzeReturnText()` 内置线程安全 LRU 缓存（100 条上限），相同退货文本避免重复调用 embedding API。

| 场景 | 冷查询 | 缓存命中 | 加速 |
|------|--------|---------|------|
| escalation 类 | 562ms | 3ms | **184x** |
| strict_reject 类 | 190ms | 3ms | **58x** |
| auto_approve 类 | 190ms | 3ms | **63x** |

### 5.2 工具输出精简

`analyzeReturnText` 输出从完整分组 JSON（~600 chars）压缩为精简格式（~150-240 chars）：

```json
{"escalation":[{"k":"投诉","s":0.383}],"hints":["升级词命中，需语境判断"]}
```

| 查询类型 | 旧输出 | 新输出 | 节省 |
|----------|--------|--------|------|
| escalation | ~600 chars | 128 chars | **79%** |
| strict_reject | ~650 chars | 242 chars | **63%** |
| auto_approve | ~750 chars | 232 chars | **69%** |

每次审核减少 ~400 chars token 消耗。

### 5.3 稳定性

5 条用例 × 3 轮测试：关键词 100% 稳定，分数范围 0.000（确定性）。HNSW 索引 + DashScope embedding 组合输出完全一致。

### 5.4 权重运行时覆盖

`/rag-test/hybrid/search` 支持 `?vw=&kw=&vt=&vtk=` 参数覆盖当前配置值，`scan_weights.py` 通过 HTTP 扫描最优权重（3 分钟扫 9 组，无需重启）。

### 5.5 性能指标采集

`ReturnAgentService` 每次审核后输出结构化指标：延迟、工具调用估算、短路触发、few-shot 触发。配套 `analyze_agent_metrics.py` 分析脚本。

---

## 六、架构记录

| 维度 | 现状 |
|------|------|
| Agent 架构 | ReAct（单 Agent + 判定框架 prompt） |
| 客服 Agent | SupervisorAgent + 4 子Agent（post_sales 为 ReAct） |
| 退货 Agent | 单 ReactAgent，7 工具，prompt 内置短路逻辑 |
| 检索封装 | Agent 不直接调精确/向量，通过 `analyzeReturnText` 封装 |
| 精确路位置 | 被 `hybridKeywordSearch` 和 `analyzeReturnText` 内部调用 |
| Plan-Execute 模式 | 已回退，设计思路保存至 `docs/plan_execute_patterns.md` |
| Few-shot | 已加入 return_audit_agent 和 post_sales_agent prompt |
| 同义词补充 | 30+ 条，覆盖 7 个规则类别，8 条用例修复已验证 |

---

## 七、变更日志

| 日期 | 变更 |
|------|------|
| 2026-05-29 | 退货 Agent Plan-Execute→ReAct；post_sales 升级 ReAct |
| 2026-05-29 | 新增时间维度测试 20 条；测试用例 25→96 条 |
| 2026-05-29 | 混合检索权重 v3 训练（废弃）；保持 v2 |
| 2026-05-29 | 补充 rules.json 同义词 30+ 条，精确路 Recall@1 +8.3pp |
| 2026-05-29 | 新增 few-shot 语义映射到 Agent prompt |
| 2026-05-29 | 精确路 8 条同义词修复验证通过 |
| 2026-05-29 | 全索引 IVF_FLAT→HNSW，冷启动退化 0 |
| 2026-05-29 | 新增 scan_weights.py 权重实时扫描，/hybrid/search 支持运行时覆盖 |
| 2026-05-29 | Agent 层 LRU 缓存（184x 加速）+ 工具输出精简（~70% 节省） |
| 2026-05-29 | 新增 analyze_agent_metrics.py + test_consistency.py |
| 2026-05-31 | 三层漏斗职责分离: L2 管≤3字(同义+错字) L3 管≥4字(相对编辑距离) |
| 2026-05-31 | L3 首次正确命中(4例): 质良问题→质量问题、不想要子→不想要了 等 |
| 2026-05-31 | autoApproveRules 索引补充, 精确覆盖率 49%→60% (+11pp) |
| 2026-05-31 | 测试用例 96→112 条（新增拼写错误 16 条）；评估改为文本匹配 |
